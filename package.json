{
  "name": "openproject",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "cd frontend && npm install",
    "test": "cd frontend && npm12 test && cd ..",
    "tslint_typechecks": "cd frontend && npm run tslint_typechecks && cd ..",
    "serve": "cd frontend && npm12 run serve",
    "serve-public": "cd frontend && ./node_modules/.bin/ng12 serve --host 0.0.0.0"
  },
  "private": true,
  "engines": {
    "node": "~10.15.3"
  }
}
